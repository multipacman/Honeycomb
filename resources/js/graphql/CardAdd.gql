mutation CardAdd($listId: ID!, $title: String!, $order: Int!) {
    cardAdd(
        input: { title: $title, order: $order, list_id: $listId, owner_id: 1 }
    ) {
        id
        title
        order
    }
}
